{
  "name": "runku-workspace",
  "version": "0.5.4",
  "private": true,
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.18.1",
  "engines": {
    "node": ">=20.18.1"
  },
  "scripts": {
    "check:packages": "pnpm --filter @runku/client check && pnpm --filter @runku/server check && pnpm --filter @runku/react check && pnpm --filter @runku/cli check",
    "check:release": "node scripts/verify-release.mjs",
    "check:sdk-release": "node scripts/verify-sdk-release.mjs",
    "check:examples": "pnpm --filter @runku/example-chat-next check && pnpm --filter @runku/example-field-board-next check && pnpm --filter @runku/example-node-actions check",
    "docs:dev": "pnpm --filter @runku/docs start",
    "docs:build": "pnpm --filter @runku/docs build",
    "docs:serve": "pnpm --filter @runku/docs serve",
    "docs:typecheck": "pnpm --filter @runku/docs typecheck"
  },
  "pnpm": {
    "overrides": {
      "serialize-javascript": "7.1.1"
    },
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "unrs-resolver"
    ]
  }
}
