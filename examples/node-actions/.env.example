# `runku dev` creates the ignored .env.local with local URL, target and keys.
# Remote environments may provide the same canonical variables explicitly.
RUNKU_URL=
RUNKU_TARGET=
RUNKU_KEY=
RUNKU_SECRET_KEY=
