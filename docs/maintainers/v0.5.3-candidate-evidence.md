# Runku 0.5.3 candidate evidence

Status: published. Source, package, storage, runtime, database, exact OCI candidate, hosted native
distribution, packaged Self-Hosted conformance, coordinated npm publication, final OCI image, Git
tag, and GitHub Release are complete.

Evidence date: 2026-09-08

## Reviewed source baseline

| Item | Value |
|---|---|
| Implementation/version commit | `9285c1c80c432f47c50d76d6b52cb57d13270915` |
| Exact candidate source commit | `a2e69a9685daf59c44f776a4bd17a6d886ad612a` |
| Integration branch | `agent/release-v0.5.3-queryable` |
| Coordinated version | `0.5.3` |
| Previous published distribution | signed `v0.5.1` at `062439a78362a5b15490ab0dd1a6358d3e502d4b` |
| Candidate tag | `ghcr.io/aldemi-tech/runku-server:0.5.3-candidate-a2e69a9685da` |
| Commit tag | `ghcr.io/aldemi-tech/runku-server:sha-a2e69a9685daf59c44f776a4bd17a6d886ad612a` |
| Multi-platform digest | `sha256:586c160c39c2ac05fd7178537cda0d987088f6bfa4d31abdf6613829344946b0` |
| Platforms | `linux/amd64`, `linux/arm64` |
| Candidate workflow | `34296823118` |
| Pre-tag distribution workflow | `34299389045` |
| Final distribution commit | `1c54613a81ab97f659534e56e8fbc81f3b670991` |
| Signed tag | `v0.5.3` |
| Final workflow | `34355132949` |
| Final multi-platform digest | `sha256:ab04449d48a8a11036dd91950af109780e7f81c00b1fa56114b4958324a38b38` |

Version `0.5.2` remains an unpublished local candidate. Version `0.5.3` coordinated that prepared
Full Node work with the additive data-query/storage contract; no existing tag or registry version
was moved or reused.

Both architecture manifests carry SBOM/provenance attestations and identify version `0.5.3` plus
the exact source revision. The candidate workflow passed all six native CLI builds and both Linux
server builds before publishing the immutable multi-platform image. Pulling that image by digest
on Linux ARM64 exposed revision `a2e69a9685daf59c44f776a4bd17a6d886ad612a`, license `Apache-2.0`,
and an embedded CLI that reported `runku 0.5.3`.

The separate pre-tag workflow rebuilt and smoke-checked the same six CLI targets and two server
targets, packed every public JavaScript package without publishing it, and exercised the compact
Self-Hosted archive on a clean Linux runner. Invitation/login, authenticated
build/publish/release/promote, Product invocation, scoped logs, coordinated PostgreSQL/object-byte
backup, offline verification, empty-install restore, persisted session, and automatic Channel
serving all passed. Its publication jobs for npm, OCI, tags, and GitHub Releases were skipped.

## Data model and lifecycle result

- `ctx.db.query(table, options?)` is one typed contract for physical table paging, logical ordered
  indexes, word-search indexes, and bounded fallback scans. Omitted options select a 100-document
  page in stable `$createdAt DESC, $id DESC` keyset order; the public maximum is 200.
- Arbitrary unindexed filters and sorts inspect at most 2,000 documents before returning
  `DATA_QUERY_REQUIRES_INDEX`. Equality prefixes, ordered suffixes, and `gt`/`gte`/`lt`/`lte`
  bounds select and narrow matching ordered indexes. `contains` remains a bounded substring/array
  fallback; `search` requires a declared whole-word index.
- Tables default to `queryable`; `keyValue` restricts them to point access and writes. Both modes
  retain one canonical document representation. Moving from `keyValue` to `queryable` registers
  projections and performs bounded, idempotent, resumable backfill before the Release is servable.
- Building and active Release catalogs participate in dual writes. Compatible older Release
  views preserve unknown fields on replace, so concurrent Releases can use additive schema views
  over the same records. Version 0.5.3 intentionally does not garbage-collect retired index rows.
- Function declarations now require only `handler`; authentication, visibility, capabilities,
  arguments, and return contracts have stable safe defaults. New string bounds count Unicode code
  points through `minLength`/`maxLength`; byte bounds remain exclusive to `v.bytes` while legacy
  persisted string-byte contracts remain decodable.

## Database result

- SQLite schema versions 3 and 4 add the physical document scan and durable index registry.
- PostgreSQL schema versions 4 through 6 add the same scan/registry plus a durable database mode.
  `dedicated` is the default and binds an empty database atomically to one exact Environment;
  `shared` is explicit and retains Project/Environment scope in every logical key.
- A real PostgreSQL 16 container passed common store conformance, concurrent OCC, repeatable-read
  snapshots, bounded pool health, Query, Mutation, dedicated Product binding, restart, and
  Management readiness checks. The concurrent first-binding campaign exposed a bootstrap race;
  bounded portable migration retry was added and the complete Product campaign then passed.
- A real single-node YugabyteDB YSQL `2026.1.1.1-b2` container passed the common logical-store
  contract plus Query and Mutation execution. The exact image used was
  `yugabytedb/yugabyte@sha256:3926eedf0ff4b99426fbff533128ff16635ed5697a5cf086dde4fafec28e6f75`.
  This proves adapter compatibility, not RF=3 failure tolerance, performance, backup/restore RPO,
  or multi-node production qualification.

## Local acceptance

| Gate | Result |
|---|---|
| `make ci-check` | passed; all Rust targets/features compile and all public JS packages are coherent |
| affected Rust behavioral suite | passed across schema, storage, execution, runtime, lifecycle, server, gateway, and Realtime |
| `make lint` and `make incomplete-check` | passed with warnings denied |
| `make docs` plus final Docusaurus rebuild | passed with no broken links/anchors after correction |
| `make storage-check` | passed against real PostgreSQL 16 |
| PostgreSQL Query and Mutation engine conformance | passed against the same real PostgreSQL instance |
| `make product-postgres-check` | passed after the migration-bootstrap race fix |
| `make product-yugabyte-check` | passed against real YugabyteDB YSQL `2026.1.1.1-b2` |
| YugabyteDB Query and Mutation engine conformance | passed against the same real YSQL instance |
| `make selfhost-package-check` | passed, including all compact Compose projections |
| `make security-audit` | passed; RustSec reported only three allowed unmaintained warnings, while pnpm reported three moderate findings and the two exact documented temporary high-severity image-size exceptions |
| optimized CLI build and smoke | passed; `target/release/runku --version` reported `runku 0.5.3` and help rendered |
| `git diff --check` and release/SDK metadata verifiers | passed for `v0.5.3` and `sdk-v0.5.3` |

## Known boundaries

- Runku still admits one active application writer per Environment. Distributed SQL does not make
  Product serving, Realtime dispatch, or scheduling active-active.
- Shared database mode is logical multitenancy, not per-Environment role isolation. Operators must
  bound pools, credentials, placement, noisy neighbors, backup, and restore for the shared tier.
- Search is normalized case-insensitive whole-word lookup with at most 4,096 distinct words per
  document. It does not provide phrase search, stemming, substring search, relevance ranking, or
  arbitrary secondary sorts.
- Applying the additive PostgreSQL/SQLite migrations makes binary downgrade unsupported. Take and
  verify a coordinated backup before upgrading.
- The local Yugabyte campaign used RF=1. Production Yugabyte requires a separately reviewed
  multi-node topology, RF/failure-domain evidence, monitoring, backup, restore, and measured load.

## Publication outcome

No credentials for a separately operated remote cell were available on the acceptance host, so no
new AWS/remote-cell campaign is claimed. The exact Product changes were instead exercised against
real PostgreSQL 16, real YugabyteDB YSQL, the digest-pinned candidate container, and the clean Linux
Self-Hosted artifact lifecycle described above.

After fresh release-owner approval, signed `v0.5.3` triggered the coordinated distribution. The
final workflow `34355132949` completed successfully and published the npm package set, final
multi-platform OCI image, downloadable native/Self-Hosted assets, attestations, and GitHub Release.
