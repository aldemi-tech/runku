# Runku 0.5.4 candidate evidence

Status: the first OCI-only candidate was published and deployed only to the QA Environment. Its
online preflight exposed one additional local-hybrid compatibility defect. A replacement source
candidate is prepared locally; publishing that replacement requires fresh release-owner approval.
No npm package, Git tag, or GitHub Release has been created for 0.5.4.

Evidence date: 2026-09-10

## Candidate identity

| Item | Value |
|---|---|
| Version | `0.5.4` |
| Replacement source commit | `1eb47d1a262d48c79b21d74b8b1b26525f48207b` |
| First candidate source | `d581532ddd01ea8fb99e61fd3fcc8bf7795bfa38` |
| First candidate workflow | `34490316158` |
| First candidate OCI digest | `sha256:6c811085b33d153a2e2fd9ad941631d759e5f6e33cfa46c5700ce1984a40693d` |
| Compatibility | additive HTTP NDJSON transports and local/OCI hybrid artifact readers; persisted data unchanged |

The source adds public `POST /v1/query/follow` over the existing Query dependency registry and
Management `POST .../data/query/follow` over committed table-change notifications. Management
streams require an immutable `release:<id>` target, retain the bounded logical planner, recheck
operator authorization, terminate after at most one hour, and replace the page after lag.

The candidate also corrects compatibility preflight, Release freeze, and Management catalog reads
for `HybridOciArtifactV1`: those paths validate the outer artifact and inspect its embedded resource
bundle instead of attempting to decode the hybrid bytes as a Safe V8 bundle. Safe-only artifacts
retain their existing path and pure Node OCI descriptors remain outside these catalog readers.

The replacement additionally accepts a canonical local `NodeEsmBundleV1` when its manifest is
`runku-hybrid`. This is the immutable format produced by the Development workspace before an OCI
publication step. Compatibility now validates it with `ensure_local_full_node_supported`, verifies
the exact bundle bytes, and then reads the same content-addressed contracts and schema resources.

## Local validation

The following gates completed successfully from the recorded source state:

- `cargo fmt --all -- --check`
- `cargo check -p runku-server`
- `cargo test -p runku-compatibility --locked` — 7 passed, including local and OCI hybrid readers
- `cargo test -p runku-server product::tests::console_catalog_and_data_admin_are_exact_idempotent_and_occ_safe --locked -- --exact` — 1 passed
- `cargo test -p runku-gateway --test websocket_realtime` — 5 passed
- `cargo test -p runku-management-service --test auth_http console_product_http_enforces_independent_least_privilege_capabilities -- --exact` — 1 passed
- `cargo test -p runku-server console_catalog_and_data_admin_are_exact_idempotent_and_occ_safe` — 1 passed
- `pnpm check:packages`
- `GITHUB_REF=refs/tags/v0.5.4 pnpm check:release`
- `git diff --check`

The real-socket Gateway test proves initial and updated HTTP Query state plus subscription cleanup.
The Management test proves `data:read` least privilege, immutable-target enforcement, initial state,
and committed update delivery. The Product adapter test covers the target pinning boundary.

The exact 62,178-byte artifact and 3,237-byte manifest from QA Release
`rel_01M24GPZTXHK5FDA4SEJAR08RG` reproduce `COMPATIBILITY_RELEASE_INVALID` on the first candidate and
load successfully through `ReleasePackage` with the replacement source. The failed preflight
advanced the Release only from `created` to resumable `building`; serving policy revision 1 and its
100% target were unchanged.

## Remaining gates

This record is not publication authority. After fresh explicit approval, create a replacement
immutable OCI-only candidate from this source, deploy it only to the QA Environment, resume the
hybrid preflight/freeze/routing campaign, record exact workflow and OCI digest evidence, and obtain
the required final approval before creating or pushing the signed `v0.5.4` tag.
