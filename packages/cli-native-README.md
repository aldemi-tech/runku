# Runku CLI native package

This package contains one operating-system and architecture-specific `runku` executable. It is an
implementation dependency of `@runku/cli`; install the public launcher instead of depending on this
package directly.

Every native package is assembled from the same binary published in the corresponding GitHub
Release archive. The package version must exactly match `@runku/cli`.
